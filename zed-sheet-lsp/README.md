# Zed-Sheet-Lsp (Placeholder)

This sub-crate under tabular-work will contain the actual Zed-specific LSP integration code.

**Role:** Hover, completion, diagnostics for tabular data in Zed editor. Pipeline-aware code actions based on `.nustage.json` sidecar.

**Dependency:** depends on nustage library (see Cargo.toml)

**Note:** The original repo (`/home/crombo/projects/zed-sheet-lsp`) didn't exist physically — the JSON fixture was stale artifact reference. This directory is now placeholder awaiting real code creation.

---
*Placeholder under tabular-work consolidation — July 2026*