[package]
name = "sro-pivot"
version = "0.1.0"
edition = "2024"
description = "Reproducible group-by/sum pivot for Syteline ToExcel exports (escape from Excel)"

[dependencies]
calamine = { workspace = true, features = ["dates"] }
clap = { workspace = true }
chrono = { version = "0.4", features = ["serde"] }
anyhow = "1.0"
csv = "1.3"
