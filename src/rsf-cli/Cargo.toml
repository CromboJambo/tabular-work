[package]
name = "rsf-cli"
version = "0.2.0"
edition = "2021"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
csv = "1.3"
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"

# CSV parser + ranked spreadsheet format. Companion to core stack — optionally depends on nustage for pipeline-aware analysis.