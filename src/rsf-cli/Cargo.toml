[package]
name = "rsf-cli"
version = "0.2.0"
edition = "2021"
license = "AGPL-3.0-or-later"
description = "Ranked Spreadsheet Format - Deterministic column ordering based on cardinality"
authors = ["Dustin Grover <dustinjgrover@gmail.com>"]
keywords = ["csv", "spreadsheet", "data", "ranking", "cardinality"]
categories = ["command-line-utilities", "data-structures"]

[dependencies]
clap = { version = "4.5", features = ["derive"] }
colored = "3.1"
crossterm = "0.28"
csv = "1.3"
ratatui = { version = "0.29", default-features = false }
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"
anyhow = "1.0"
encoding_rs_io = "0.1.7"
regex = "1.10"
tui-textarea = "0.7"

# Semantic model layer (pure representation, no CLI/Excel/Git knowledge)
rsf = { path = "../rsf-core" }

[profile.release]
strip = true
opt-level = "z"
lto = true
