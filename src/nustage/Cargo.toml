[package]
name = "nustage"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = "1.0"
toml = "0.8"

# Pipeline engine: canonical transformation model, sidecar management (.nustage.json), execution
# No dependencies on other two (zed-sheet-lsp, git-sheets) — compose at workflow level